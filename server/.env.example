# .env.example

# JWT secret
JWT_SECRET=change_this_to_a_random_string

# SMTP (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your@gmail.com
SMTP_PASS=your_app_password
FROM_EMAIL="Navarro <no-reply@navarro.local>"

ADMIN_EMAIL=admin@navarro.local

PORT=4000
